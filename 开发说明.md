# 用户登录页面 - 开发说明文档

## 📋 基本信息
- **需求ID**: 3006
- **原型ID**: 2175
- **页面类型**: 用户登录页面
- **创建日期**: 2026-02-28

---

## 🎨 设计规范

### 颜色方案
```css
/* 主色调 */
--primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
--primary-color: #667eea;

/* 文字颜色 */
--text-primary: #1a1a1a;
--text-secondary: #374151;
--text-muted: #6b7280;

/* 边框和背景 */
--border-color: #e5e7eb;
--bg-white: #ffffff;

/* 状态颜色 */
--color-error: #ef4444;
--color-success: #10b981;
--color-error-bg: #fef2f2;
```

### 间距规范
- 卡片圆角: 12px
- 输入框圆角: 8px
- 表单项间距: 20px
- 卡片内边距: 48px 40px

---

## 📝 表单字段说明

### 1. 账号输入框
| 属性 | 值 |
|-----|-----|
| 类型 | text |
| ID | username |
| name | username |
| placeholder | 请输入用户名/邮箱/手机号 |
| 必填 | 是 |
| 最小长度 | 3个字符 |

### 2. 密码输入框
| 属性 | 值 |
|-----|-----|
| 类型 | password（可切换） |
| ID | password |
| name | password |
| placeholder | 请输入密码 |
| 必填 | 是 |
| 最小长度 | 6个字符 |
| 特殊功能 | 密码可见性切换 |

---

## ✅ 表单验证规则

### 账号验证
```javascript
// 必填
if (!username) {
  return '请输入账号';
}

// 长度验证
if (username.length < 3) {
  return '账号长度至少为3位';
}
```

### 密码验证
```javascript
// 必填
if (!password) {
  return '请输入密码';
}

// 长度验证
if (password.length < 6) {
  return '密码长度至少为6位';
}
```

---

## 🔌 API 接口定义

### 登录接口
```javascript
POST /api/login

// 请求参数
{
  "username": "string",  // 用户名/邮箱/手机号
  "password": "string"   // 密码
}

// 成功响应
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "string",
    "userInfo": {
      "id": "string",
      "username": "string",
      "email": "string",
      "avatar": "string"
    }
  }
}

// 失败响应
{
  "code": 401,
  "message": "账号或密码错误"
}
```

---

## 🎯 交互功能清单

### 核心功能
- [x] 账号输入和验证
- [x] 密码输入和验证
- [x] 密码可见性切换（眼睛图标）
- [x] 表单提交
- [x] 错误提示（字段级 + 全局级）
- [x] 加载状态显示

### 交互细节
- [x] 页面加载时自动聚焦到账号输入框
- [x] 输入时自动清除错误提示
- [x] 失去焦点时触发验证
- [x] 错误时自动聚焦到第一个错误输入框
- [x] 密码错误后清空密码框并聚焦
- [x] Escape 键清空表单

### 动画效果
- [x] 页面淡入动画（fadeInUp）
- [x] 按钮加载旋转动画
- [x] 错误提示抖动动画
- [x] 成功提示滑入动画

---

## 📱 响应式设计

### 断点设置
```css
/* 移动端 */
@media (max-width: 480px) {
  .login-container {
    padding: 32px 24px;
  }

  .login-header h1 {
    font-size: 24px;
  }
}
```

---

## 🔐 安全要求

### 必须实现
1. ✅ HTTPS 密码传输
2. ✅ 密码加密存储（bcrypt）
3. ✅ JWT Token 认证
4. ✅ 防暴力破解机制
5. ✅ XSS 防护
6. ✅ CSRF 防护

### 建议实现
- 验证码功能
- 登录次数限制
- IP 黑名单
- 异地登录检测

---

## 🧪 测试账号

### 原型测试账号
```
账号: admin
密码: admin123
```

### 测试场景
1. ✅ 正常登录（正确账号密码）
2. ✅ 账号错误
3. ✅ 密码错误
4. ✅ 账号为空
5. ✅ 密码为空
6. ✅ 账号长度不足
7. ✅ 密码长度不足

---

## 📂 建议文件结构

```
login/
├── index.html          # 页面结构
├── login.css           # 样式文件
├── login.js            # 业务逻辑
└── login.test.js       # 测试文件
```

---

## 🚀 开发步骤

### 第一步：页面结构
1. 创建 HTML 页面
2. 添加表单元素
3. 设置表单属性

### 第二步：样式实现
1. 实现渐变背景
2. 创建卡片式布局
3. 添加表单样式
4. 实现响应式设计

### 第三步：交互逻辑
1. 实现表单验证
2. 添加错误提示
3. 实现密码切换
4. 添加加载状态

### 第四步：接口对接
1. 对接登录 API
2. 处理登录成功
3. 处理登录失败
4. Token 存储和管理

### 第五步：测试优化
1. 功能测试
2. 兼容性测试
3. 性能优化
4. 安全加固

---

## 📊 优先级说明

### P0 - 核心功能（必须完成）
- [ ] 表单布局和样式
- [ ] 表单验证逻辑
- [ ] 登录 API 对接
- [ ] 错误提示显示

### P1 - 重要功能（应该完成）
- [ ] 密码可见性切换
- [ ] 加载状态处理
- [ ] 响应式适配
- [ ] Token 管理

### P2 - 增强功能（建议完成）
- [ ] 动画效果
- [ ] 键盘快捷键
- [ ] 记住密码
- [ ] 自动登录

### P3 - 扩展功能（可选）
- [ ] 第三方登录
- [ ] 多语言支持
- [ ] 主题切换
- [ ] 忘记密码

---

## 📖 参考资料

- 原型文件: `/home/meta/maifast/ai_dev_temp/3006/2175.html`
- 详细分析报告: `页面分析报告.md`

---

**最后更新**: 2026-02-28
